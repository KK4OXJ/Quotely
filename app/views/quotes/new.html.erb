<%= link_to '〈', quotes_path, :class => 'link-back' %>

<div class="form-container">
  <%= form_for(@quote) do |f| %>
    <%= f.label "Write your Quote:", :for => 'quote' %>
    <br>
    <%= f.text_area :content, :id => 'quote', :autofocus => 'true' %> 
    <br>
    <%= f.submit "Create", :class => 'btn' %> 
  <% end %>
</div>