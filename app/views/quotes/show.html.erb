<%= link_to '〈', quotes_path, :class => 'link-back' %>

 <div class="current-quote-container">
  <div class="current-quote">
    <span class="quote-mark">&ldquo;</span>
    <span class="quote-content"><%= @quote.content %></span>
    <span class="quote-mark">&rdquo;</span>
    <br><br>
    <small><%= @user.email %><%= link_to "Edit", edit_quote_path %> | <%= link_to "Delete", quote_path(@quote), method: :delete, data: {confirm: "Are you sure you want to delete your Quote? You can't undo this."} %></small>
  </div>
</div>